const width = document.getElementById("width")
const height = document.getElementById("height")


width.innerHTML = "the window width is" + window.innerWidth
height.innerHTML = "the window height is" + window.innerHeight